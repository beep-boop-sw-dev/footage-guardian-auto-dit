[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "footage-guardian-auto-dit"
version = "0.1.0"
description = "Local-first footage safeguarding for travelling videographers"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
footage-guardian-auto-dit = "footage_guardian.cli:main"

[tool.setuptools.packages.find]
include = ["footage_guardian*"]

